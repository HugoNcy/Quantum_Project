# Quantum stack -- versions validated by the team, do not bump casually
# (see the project explanation document: API traps were found on these exact versions)
perceval-quandela==1.2.3
merlinquantum==0.4.0

# torch is installed separately in the Dockerfile (CPU wheel, smaller image).
# If you install locally without the container, uncomment:
# torch==2.5.1

# Scientific / plotting
numpy
matplotlib
pandas

# Notebooks
jupyterlab
